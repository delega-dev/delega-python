name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    # Calls the reusable workflow with the full 3.9-3.13 matrix for PRs.
    uses: ./.github/workflows/test.yml
